import java.util.*;
import java.math.*;
import static java.lang.Math.*;

public class SimilarUserDetection {
	
	public String haveSimilar(String[] handles) {
		//Map<String,int>;
		handles[0].replace('0', 'o');
		handles[0].replace('1', 'l');
		handles[0].replace('I', 'l');
	}
}
